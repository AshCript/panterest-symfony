{% extends 'base.html.twig' %}

{% block title 'Editing pin ' ~ pin.id %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1>Editing Pin {{ pin.id }}</h1>
            {{ include('partials/_form.html.twig', {'submitButtonText': 'Update'}) }}
        </div>
    </div>
{% endblock %}
